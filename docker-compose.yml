version: '3.8'

networks:
  shared-comms-network:

services:
  express-backend-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    restart: unless-stopped
    networks:
      - shared-comms-network
    environment:
      - MONGO_URI=mongodb+srv://raj:NwAXvGMLWpVGFIs2@cluster0.v1bry.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
      - PORT=8080
